import { HoppersFilter } from "api/filters/hoopers"

type FilterHoppersProps = {
    filter: HoppersFilter
    onChange: (filter: Partial<HoppersFilter>) => void
}

export default function FilterHoppers(props: FilterHoppersProps) {
    return <div></div>
}
